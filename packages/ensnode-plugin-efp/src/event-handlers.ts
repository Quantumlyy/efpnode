/**
 * Placeholder ENSIndexer event-handlers entry-point. The real implementation
 * lives in the next commit — this stub exists so the package's `exports` map
 * resolves during typechecking even before the handlers are wired up.
 */
export default function attachEFPHandlers(): void {
  // Intentionally empty until handlers/* are registered.
}
