/**
 * Placeholder ENSIndexer plugin definition. The real implementation lives in
 * the next commit — this stub keeps the file path stable so the README and
 * drop-in instructions don't churn while the rest of the plugin is built.
 *
 * @see docs/RESEARCH.md "How this becomes a unified node"
 */
export {};
